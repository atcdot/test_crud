{% extends '::base.html.twig' %}

{% block body -%}
    <h1>UserRoles list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                {#<th>Userid</th>#}
                <th>Rolename</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('userroles_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                {#<td>{{ entity.userId }}</td>#}
                <td>{{ entity.roleName }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('userroles_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('userroles_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('userroles_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
