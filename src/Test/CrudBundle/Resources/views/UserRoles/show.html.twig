{% extends '::base.html.twig' %}

{% block body -%}
    <h1>UserRoles</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            {#<tr>#}
                {#<th>Userid</th>#}
                {#<td>{{ entity.userId }}</td>#}
            {#</tr>#}
            <tr>
                <th>Rolename</th>
                <td>{{ entity.roleName }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('userroles') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('userroles_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
