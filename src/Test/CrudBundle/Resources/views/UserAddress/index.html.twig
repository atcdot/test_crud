{% extends '::base.html.twig' %}

{% block body -%}
    <h1>UserAddress list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Zip</th>
                <th>City</th>
                <th>Address</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('useraddress_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.zip }}</td>
                <td>{{ entity.city }}</td>
                <td>{{ entity.address }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('useraddress_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('useraddress_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('useraddress_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
